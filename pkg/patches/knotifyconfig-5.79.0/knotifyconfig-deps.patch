--- knotifyconfig-5.79.0/CMakeLists.txt
+++ knotifyconfig-5.79.0/CMakeLists.txt
@@ -30,6 +30,11 @@
 find_package(KF5Config ${KF_DEP_VERSION} REQUIRED)
 find_package(KF5I18n ${KF_DEP_VERSION} REQUIRED)
 find_package(KF5KIO ${KF_DEP_VERSION} REQUIRED)
+find_package(KF5DBusAddons ${KF_DEP_VERSION})
+find_package(KF5Crash ${KF_DEP_VERSION})
+find_package(KF5IconThemes ${KF_DEP_VERSION})
+find_package(KF5GuiAddons ${KF_DEP_VERSION})
+find_package(KF5Archive ${KF_DEP_VERSION})
 
 find_package(Canberra)
 set_package_properties(Canberra PROPERTIES
